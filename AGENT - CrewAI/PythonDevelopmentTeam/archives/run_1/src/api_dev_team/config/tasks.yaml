code_task:
  description: >
    Tu vas créer une application en python en suivant ces instructions :
    
    Instructions
    ------------
    {app}
    
  expected_output: >
    Ta réponse finale sera le code python et uniquement le code python.

review_task:
  description: >
    En utilisant le code que tu as, vérifie s'il y a des erreurs. Vérifie tout types d'erreur :
    erreurs de syntaxe, erreur de logique, imports absents, déclarations de variables et surtout
    les failles de sécurité.

    Assure toi que la sécurité est maximale et si ce n'est pas le cas, demande des corrections.

  expected_output: >
    Ta réponse finale sera le code python et uniquement le code python.

evaluate_task:
  description: >
    Tu aides à créer une application en python en suivant ces instructions :

    Instructions
    ------------
    {app}

    Tu t'assureras que le code est complet et qu'il effectue tout ce qu'il est supposé faire.

  expected_output: >
    Ta réponse finale sera le code python et uniquement le code python.